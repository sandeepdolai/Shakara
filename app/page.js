'use client'
import {useState} from 'react'
export default function Home(){const [message,setMessage]=useState(''),[reply,setReply]=useState(''),[loading,setLoading]=useState(false);async function send(){if(!message.trim())return;setLoading(true);setReply('');try{const r=await fetch('/api/message',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message})});const d=await r.json();if(!r.ok)throw Error(d.error);setReply(`${d.reply} • ${new Date(d.receivedAt).toLocaleTimeString()}`)}catch(e){setReply(`Error: ${e.message}`)}finally{setLoading(false)}}return <><nav><b>Shakara</b><div><a href="#features">Features</a><a href="#live">Live</a><a href="#about">About</a></div><a className="button" href="#live">Try it</a></nav><main><section className="hero"><span>● Live full-stack application</span><h1>Not just a<br/>static website.</h1><p>Next.js + React on the frontend, with a real server API behind every request.</p><div><a className="button" href="#live">Open workspace</a> <a className="button secondary" href="#features">Explore</a></div></section><section id="features"><h2>Real functionality.</h2><p>Frontend and backend in one Next.js project.</p><div className="grid"><article><h3>React UI</h3><p>Interactive client components with real state and server communication.</p></article><article><h3>API Routes</h3><p>Requests execute on the server through the Next.js App Router.</p></article><article><h3>Vercel Ready</h3><p>Deploy the entire application as a full-stack Next.js project.</p></article></div></section><section id="live"><div className="panel"><span>● Server online</span><h2>Send a real request</h2><p>Type a message and call the Next.js backend.</p><div className="row"><input value={message} onChange={e=>setMessage(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Type something…"/><button className="button" onClick={send} disabled={loading}>{loading?'Sending…':'Send'}</button></div>{reply&&<pre>{reply}</pre>}</div></section><section id="about"><div className="panel"><h2>Shakara is full-stack.</h2><p>Built with the Next.js App Router, React client components, and a server API endpoint.</p></div></section></main><footer>© 2026 Shakara</footer></>}
